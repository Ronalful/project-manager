spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/project
    username: ${POSTGRESQL_LOGIN}
    password: ${POSTGRESQL_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  kafka:
    producer:
      bootstrap-servers: kafka:9092
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.type.mapping: projectNotification:com.example.project.kafka.ProjectNotification

application:
  config:
    user-url: http://auth-service:8080/api/v1/admin/users
    task-url: http://task-service:8080/api/v1/tasks
    task-assignment-url: http://task-service:8080/api/v1/task-assignments

springdoc:
  swagger-ui:
    path: "/api/v1/doc/projects/swagger-ui.html"
  api-docs:
    path: "/api/v1/doc/projects/v3/api-docs"