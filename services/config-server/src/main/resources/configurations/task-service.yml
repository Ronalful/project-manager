spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/task
    username: ${POSTGRESQL_LOGIN}
    password: ${POSTGRESQL_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  kafka:
    producer:
      bootstrap-servers: kafka:9092
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.type.mapping: taskNotification:com.example.task.kafka.TaskNotification,taskChangedStateNotification:com.example.task.kafka.TaskChangedStateNotification

application:
  config:
    user-url: http://auth-service:8080/api/v1/admin/users
    admin-project-url: http://project-service:8080/api/v1/admin/projects
    user-project-url: http://project-service:8080/api/v1/user/projects

springdoc:
  swagger-ui:
    path: "/api/v1/doc/tasks/swagger-ui.html"
  api-docs:
    path: "/api/v1/doc/tasks/v3/api-docs"